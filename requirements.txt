# ──────────────────────────────────────────────
# SlangHunter – Python Dependencies (legacy dev reference)
# ──────────────────────────────────────────────
# Upper-bound constraints added in S6 supply-chain hardening.
# For reproducible installs, prefer the curated lock files:
#   requirements-dev.lock, requirements-api.lock, requirements-ml.lock
# ──────────────────────────────────────────────

# Development / Quality tools
flake8>=6.1.0,<8.0
pytest>=7.4.0,<9.0
pytest-cov>=4.0,<6.0
mypy>=1.5,<2.0
